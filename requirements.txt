requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Technical analysis (choose one)
pandas-ta>=0.3.14b0

# LLM provider - Google Gemini API
google-genai>=1.0.0
# Legacy google-generativeai for Twitter sentiment (can be removed if Twitter scraper is updated)
google-generativeai>=0.3.0

# Twitter scraping (optional - may have compatibility issues with Python 3.11+)
# If snscrape doesn't work, try: pip install git+https://github.com/JustAnotherArchivist/snscrape.git
# Or use Python 3.10 or earlier
snscrape>=0.7.0

